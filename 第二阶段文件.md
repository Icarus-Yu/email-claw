# 🚀 EmailClaw 第二阶段工作总结 (Agent 处理层基础框架)

本阶段在第一阶段的 **邮件抓取、结构化解析与数据库持久化** 基础上，开始搭建 EmailClaw 的智能处理层。重点工作是打通从邮件入库到 Agent 分析的后端主链路，并为后续接入 OpenClaw 框架、飞书交互和真实邮件操作预留清晰接口。

---

## 🛠 已完成的核心任务

### 1. Agent 处理层目录与类型体系搭建
为了让后端从“只会抓邮件”升级为“能理解邮件”，本阶段新增了独立的 Agent 模块目录：

- `src/agents/types/`: 定义 Agent 输入输出结构。
- `src/agents/prompts/`: 存放后续给 OpenClaw/LLM 使用的系统提示词。
- `src/agents/tools/`: 存放邮件文本处理、关键词匹配等工具函数。
- `src/agents/skills/`: 存放邮件分析 Skill 的具体实现。

其中 `emailAgent.ts` 定义了邮件分析的核心数据结构，包括：

- `EmailClassificationResult`: 邮件分类结果。
- `EmailImportanceResult`: 重要性评分结果。
- `EmailSummaryResult`: 邮件摘要结果。
- `EmailAgentResult`: Agent 对一封邮件的完整分析结果。

这使得后续无论使用本地规则、OpenClaw，还是直接调用大模型 API，都可以遵循统一的数据格式。

### 2. 本地规则版 ClassificationSkill 实现
为了让 Agent 链路先跑通，本阶段实现了一个基础的本地规则 Agent：

- 根据邮件标题、发件人、正文和附件信息进行关键词匹配。
- 初步支持六类邮件分类：
  - `work`
  - `personal`
  - `shopping`
  - `marketing`
  - `spam`
  - `other`
- 支持基础重要性评分，识别“紧急、截止、会议、deadline、urgent”等信号。
- 支持生成简短邮件摘要。

该版本不是最终智能分类方案，而是一个 **可运行的兜底 Agent**。它的价值在于：即使 OpenClaw 尚未安装或调用失败，系统仍然可以完成基础分析流程，避免主链路中断。

### 3. AgentService 统一调度入口
新增 `src/backend/services/agentService.ts`，作为后端调用 Agent 的统一入口。

当前流程为：

```text
EmailService 接收到邮件
  -> 保存邮件到数据库
  -> 调用 AgentService.analyzeEmail()
  -> AgentService 执行邮件分析
  -> DatabaseService 保存分析结果
```

`AgentService` 当前具备两种能力：

- 当 OpenClaw 启用时，优先调用 OpenClaw。
- 当 OpenClaw 未启用或调用失败时，自动回退到本地规则 Agent。

这保证了系统既能逐步接入真正的 Agent 框架，又不会因为外部运行时未配置好而阻塞后端开发。

### 4. OpenClaw 接入适配层
本阶段调研并确认了 OpenClaw 的接入方式。OpenClaw 更标准的使用方式是作为本地 Agent 运行时，通过 CLI/Gateway 处理输入输出，而不是在项目代码中直接 import 一个简单 SDK。

因此新增 `src/backend/services/openClawClient.ts`，通过如下方式调用 OpenClaw：

```bash
openclaw agent --agent email-claw --message "..." --json --local
```

后端会把邮件结构化内容组织成 Prompt，发送给 OpenClaw，并要求返回固定 JSON：

```json
{
  "category": "work",
  "confidence": 0.8,
  "classificationReasoning": "分类理由",
  "importance": 7,
  "importanceReasoning": "重要性理由",
  "summary": "邮件摘要"
}
```

同时新增 workspace skill：

- `skills/email-analysis/SKILL.md`

该文件用于告诉 OpenClaw：当处理 EmailClaw 邮件分析任务时，必须返回符合约束的 JSON 数据，不能返回 Markdown 或额外解释文本。

### 5. 数据库分析结果持久化
在 `DatabaseService` 中新增 `saveEmailAnalysis()` 方法，用于保存 Agent 分析结果。

目前会写入以下数据：

- 更新 `Email.category`: 邮件分类。
- 更新 `Email.importance`: 邮件重要性评分。
- 写入或更新 `Classification`: 保存分类置信度、推理理由、工具使用记录、执行步骤和模型名称。
- 写入 `AgentLog`: 记录 Agent 执行日志，包括输入、输出、状态、模型和耗时。

这一步使系统具备了从“邮件原始数据”到“AI 分析结果”的持久化闭环。

### 6. 邮件处理主链路升级
第一阶段结束时，邮件处理流程为：

```text
IMAP 监听
  -> 邮件抓取
  -> mailparser 解析
  -> 保存 Email
  -> 标记 CLAWED
```

本阶段升级后，流程变为：

```text
IMAP 监听
  -> 邮件抓取
  -> mailparser 解析
  -> 保存 Email
  -> Agent 分析
  -> 保存分类、重要性、摘要和日志
  -> 标记 CLAWED
```

也就是说，系统已经从基础邮件同步层，推进到了智能处理层的第一版可运行架构。

---

## 📂 阶段性产出文件

- `src/backend/services/agentService.ts`: Agent 分析调度入口。
- `src/backend/services/openClawClient.ts`: OpenClaw CLI/Gateway 调用适配层。
- `src/agents/types/emailAgent.ts`: Agent 输入输出类型定义。
- `src/agents/prompts/systemPrompts.ts`: 邮件分类、重要性判断、摘要生成 Prompt 占位。
- `src/agents/tools/emailTextTools.ts`: 邮件文本归一化、关键词匹配和摘要工具。
- `src/agents/skills/classificationSkill.ts`: 本地规则版邮件分类 Skill。
- `skills/email-analysis/SKILL.md`: OpenClaw workspace skill 说明文件。
- `src/backend/services/databaseService.ts`: 新增 Agent 分析结果保存逻辑。
- `src/backend/services/emailService.ts`: 接入 AgentService，邮件入库后触发分析。
- `第二阶段文件.md`: 本阶段总结文档。

---

## 🔧 当前运行与配置方式

当前项目支持两种 Agent 运行模式。

### 1. 本地规则 Agent 模式
这是默认模式，不需要安装 OpenClaw。

系统会使用 `ClassificationSkill` 中的关键词规则完成基础分类、重要性判断和摘要生成。

适合：

- 后端流程调试。
- 数据库写入验证。
- OpenClaw 尚未配置完成时继续开发。

### 2. OpenClaw Agent 模式
当本地安装并配置好 OpenClaw 后，可以在 `.env` 中开启：

```env
OPENCLAW_ENABLED=true
OPENCLAW_COMMAND=openclaw
OPENCLAW_AGENT_ID=email-claw
OPENCLAW_TIMEOUT_MS=30000
```

此时邮件分析会优先交给 OpenClaw 处理。

如果 OpenClaw 调用失败，系统会打印 warning，并自动回退到本地规则 Agent，保证邮件处理链路不中断。

---

## ✅ 当前能力边界

本阶段已经完成：

- 邮件入库后自动进入 Agent 分析流程。
- 能生成分类、重要性评分和摘要。
- 能把分析结果写回数据库。
- 能记录 Agent 执行日志。
- 已预留 OpenClaw 接入方式。
- 已创建 OpenClaw workspace skill。
- TypeScript 编译验证通过。

本阶段尚未完成：

- OpenClaw 本地运行时安装与模型 API 配置。
- 真实 LLM 分类效果调优。
- 飞书机器人消息推送。
- 飞书按钮 Webhook 回调处理。
- 点击“删除、已读、归档、标为重点”等按钮后对真实邮箱执行 IMAP 操作。
- 用户反馈闭环和分类策略持续优化。

---

## ⏭ 下一阶段规划：飞书交互与真实邮件操作

1. **安装并配置 OpenClaw**
   - 安装 OpenClaw CLI。
   - 配置模型 API。
   - 验证 `openclaw agent --json --local` 能返回规范 JSON。
   - 确认 `skills/email-analysis/SKILL.md` 能被 OpenClaw 加载。

2. **飞书推送服务**
   - 编写 `FeishuService`。
   - 在重要邮件到达后推送飞书卡片。
   - 卡片中展示邮件标题、发件人、摘要、分类和重要性。

3. **飞书按钮回调 Webhook**
   - 新增飞书回调路由。
   - 接收用户点击事件。
   - 解析按钮 action 和 emailId。

4. **真实邮箱操作能力**
   - 在 `EmailService` 中新增邮件操作方法：
     - 标记已读。
     - 删除邮件。
     - 归档邮件。
     - 标为重点。
   - 将飞书按钮操作同步到 IMAP 邮箱和本地数据库。

5. **反馈闭环**
   - 支持用户反馈分类是否正确。
   - 将反馈写入 `Classification.feedback`。
   - 后续用于优化 Prompt 或分类策略。

---

## 📌 阶段总结

第二阶段的核心成果是：**搭建了 EmailClaw 的 Agent 处理层基础框架，并将其接入邮件处理主链路。**

目前系统已经具备如下完整链路：

```text
邮件到达
  -> IMAP 抓取
  -> MIME 解析
  -> PostgreSQL 入库
  -> Agent 分类/评分/摘要
  -> 分析结果持久化
  -> IMAP 标记处理完成
```

这为后续接入 OpenClaw 的真实多轮推理能力、飞书交互卡片和用户反馈闭环打下了基础。
