# EmailClaw 飞书前端开发对接文档

本文档面向负责飞书端交互的前端同学。当前飞书前端从零开始，因此本阶段目标不是做完整 Web 页面，而是先完成 **飞书机器人卡片交互闭环**。

---

## 一、整体目标

飞书端负责作为用户交互入口：

```text
后端抓取邮件
  -> Agent 分析邮件
  -> 后端推送飞书卡片
  -> 用户在飞书卡片点击按钮
  -> 飞书回调后端
  -> 后端操作数据库和真实邮箱
  -> 后端更新飞书卡片或回复处理结果
```

飞书前端不直接操作邮箱，也不直接访问数据库。  
飞书前端只负责：

- 展示后端推送的邮件分析结果。
- 提供用户操作按钮。
- 将用户点击事件回传给后端。

---

## 二、前端当前需要完成什么

当前前端从零开始，建议按以下顺序开发。

### 1. 创建飞书机器人应用

需要在飞书开放平台完成：

- 创建企业自建应用。
- 开启机器人能力。
- 获取应用凭证：
  - `FEISHU_APP_ID`
  - `FEISHU_APP_SECRET`
- 配置事件订阅或卡片回调地址。

后端后续会提供：

```http
POST /api/feishu/webhook
```

作为飞书事件和卡片按钮回调入口。

### 2. 设计邮件通知卡片

每当后端分析完一封邮件后，会向飞书推送一张邮件卡片。

卡片需要展示：

```text
邮件标题
发件人
收件人
收到时间
AI 分类
重要性评分
邮件摘要
分类理由
```

建议卡片视觉结构：

```text
[重要邮件] 项目会议通知

发件人：teacher@example.com
收件人：me@example.com
时间：2026-04-25 10:00

分类：work
重要性：8 / 10

摘要：
明天下午三点讨论项目需求。

分类理由：
邮件涉及项目会议和需求讨论。

[标为已读] [标为重点] [归档] [删除]
[分类正确] [分类错误] [查看详情]
```

### 3. 约定卡片数据字段

后端推送给飞书卡片的数据建议使用以下结构。

```json
{
  "emailId": "email-db-id",
  "from": "teacher@example.com",
  "to": "me@example.com",
  "subject": "项目会议通知",
  "receivedAt": "2026-04-25T10:00:00.000Z",
  "category": "work",
  "importance": 8,
  "summary": "明天下午三点讨论项目需求。",
  "classificationReasoning": "邮件涉及项目会议和需求讨论。",
  "confidence": 0.9,
  "isRead": false,
  "isArchived": false
}
```

字段说明：

| 字段 | 类型 | 说明 |
|---|---|---|
| `emailId` | string | 数据库中 `Email.id`，按钮回调必须携带 |
| `from` | string | 发件人 |
| `to` | string | 收件人 |
| `subject` | string | 邮件标题 |
| `receivedAt` | string | 邮件接收时间，ISO 格式 |
| `category` | string | AI 分类结果 |
| `importance` | number | 重要性评分，0-10 |
| `summary` | string | 邮件摘要 |
| `classificationReasoning` | string | 分类理由 |
| `confidence` | number | 分类置信度，0-1 |
| `isRead` | boolean | 是否已读 |
| `isArchived` | boolean | 是否已归档 |

---

## 三、卡片按钮 Action 约定

飞书卡片按钮点击后，必须把 `action` 和 `emailId` 回传给后端。

基础 payload：

```json
{
  "action": "mark_read",
  "emailId": "email-db-id"
}
```

当前建议支持以下按钮：

| 按钮 | action | 说明 |
|---|---|---|
| 标为已读 | `mark_read` | 将邮件标记为已读 |
| 标为重点 | `mark_important` | 将邮件重要性设为最高，并同步邮箱星标 |
| 归档 | `archive` | 归档邮件 |
| 删除 | `delete` | 删除邮件 |
| 分类正确 | `feedback_correct` | 告诉系统 AI 分类正确 |
| 分类错误 | `feedback_wrong` | 告诉系统 AI 分类错误 |
| 查看详情 | `view_detail` | 查看邮件完整内容 |
| 重新分析 | `reanalyze` | 重新触发 Agent 分析 |

注意：

- `emailId` 必须存在。
- `action` 必须使用上表约定值。
- 不建议前端自己编临时 action 名称，否则后端无法识别。

---

## 四、分类错误时的交互

如果用户点击“分类错误”，前端需要让用户选择正确分类。

分类枚举固定为：

```text
work
personal
shopping
marketing
spam
other
```

回调 payload 示例：

```json
{
  "action": "feedback_wrong",
  "emailId": "email-db-id",
  "expectedCategory": "personal",
  "comment": "这是朋友发来的邮件，不是工作邮件"
}
```

其中：

- `expectedCategory` 建议必填。
- `comment` 可选。

---

## 五、查看详情交互

如果用户点击“查看详情”，前端只需要回传：

```json
{
  "action": "view_detail",
  "emailId": "email-db-id"
}
```

后端收到后，可以重新发送一张详情卡片，内容包括：

```text
邮件标题
发件人
收件人
时间
完整正文或正文摘要
附件列表
AI 分类结果
重要性评分
```

注意：飞书卡片不适合展示超长正文。  
如果正文很长，建议后端只返回前若干字符，并提供“继续查看”或“打开详情”的后续方案。

---

## 六、飞书回调接口约定

后端后续会提供统一飞书回调接口：

```http
POST /api/feishu/webhook
```

飞书所有按钮点击事件都回调到这个接口。

前端/飞书侧需要保证事件中能解析出：

```json
{
  "action": "delete",
  "emailId": "email-db-id"
}
```

后端收到后会执行：

```text
解析 action 和 emailId
  -> 查询数据库中的 Email
  -> 根据 Email.uid 操作真实邮箱
  -> 更新数据库状态
  -> 返回处理结果
  -> 可选更新飞书卡片
```

---

## 七、不同 Action 的后端预期行为

| action | 后端数据库操作 | 后端邮箱操作 |
|---|---|---|
| `mark_read` | `Email.isRead = true` | IMAP 添加 `\Seen` |
| `mark_important` | `Email.importance = 10` | IMAP 添加 `\Flagged` |
| `archive` | `Email.isArchived = true` | IMAP 移动到归档目录或执行归档策略 |
| `delete` | `Email.isDeleted = true` | IMAP 添加 `\Deleted` 并 `expunge` |
| `feedback_correct` | `Classification.feedback = correct` | 无 |
| `feedback_wrong` | `Classification.feedback = incorrect` | 无 |
| `view_detail` | 无 | 无 |
| `reanalyze` | 更新分类、重要性、摘要、日志 | 无 |

说明：

- 当前数据库中还没有 `Email.isDeleted` 和独立的 `Email.summary` 字段，后端后续需要补充。
- 删除和归档属于真实邮箱操作，需要后端通过 IMAP 执行。
- 前端不需要关心 IMAP 细节。

---

## 八、当前后端状态

后端目前已经完成：

- IMAP 邮件监听。
- 邮件抓取。
- MIME 解析。
- 邮件基础信息入库。
- Agent 分析基础框架。
- 分类、重要性、摘要结果持久化。
- OpenClaw 接入适配层。

后端目前还未完成：

- 飞书消息发送服务。
- 飞书 webhook 回调路由。
- 卡片按钮真实处理逻辑。
- 邮箱删除、归档、已读、星标等 IMAP 操作接口。
- `Email.summary`、`Email.isDeleted` 等字段补充。

因此，前端当前可以先完成：

- 飞书应用创建。
- 机器人权限配置。
- 邮件卡片样式设计。
- 按钮 action 设计。
- 回调 payload 结构确认。
- 本地 mock 数据调试卡片展示。

---

## 九、前端开发建议顺序

### 第一步：用 Mock 数据做卡片

先不要等后端接口全部完成，直接用如下 mock 数据调试卡片。

```json
{
  "emailId": "mock-email-001",
  "from": "teacher@example.com",
  "to": "student@example.com",
  "subject": "项目会议通知",
  "receivedAt": "2026-04-25T10:00:00.000Z",
  "category": "work",
  "importance": 8,
  "summary": "明天下午三点讨论项目需求。",
  "classificationReasoning": "邮件涉及项目会议和需求讨论。",
  "confidence": 0.9,
  "isRead": false,
  "isArchived": false
}
```

### 第二步：把按钮 payload 固定下来

所有按钮都统一回传：

```json
{
  "action": "具体操作",
  "emailId": "邮件ID"
}
```

### 第三步：接后端 webhook

等后端实现：

```http
POST /api/feishu/webhook
```

前端/飞书侧再把卡片按钮点击事件接到这个地址。

### 第四步：联调真实流程

最终联调目标：

```text
真实邮件到达
  -> 后端抓取并分析
  -> 飞书收到卡片
  -> 用户点击按钮
  -> 后端收到回调
  -> 后端操作数据库和真实邮箱
  -> 飞书显示处理结果
```

---

## 十、前后端沟通重点

前端需要和后端确认以下内容：

1. 飞书应用的 `APP_ID` 和 `APP_SECRET` 由谁配置。
2. 飞书事件回调地址最终是什么。
3. 卡片按钮 action 是否按本文档命名。
4. 卡片中是否必须展示完整正文。
5. 分类错误时是否必须让用户选择正确分类。
6. 删除邮件是软删除还是直接真实删除。
7. 归档操作对应邮箱中的哪个文件夹。

---

## 📌 简单总结

飞书前端当前要做的不是传统网页，而是 **飞书机器人卡片交互**。

前端最重要的任务是：

```text
设计邮件卡片
  -> 展示邮件分析信息
  -> 放置操作按钮
  -> 点击按钮时回传 action + emailId
```

真实的邮件删除、归档、标记已读、标为重点等操作都由后端完成。飞书端只负责把用户指令准确传回来。
